```
  1.  다음 중 프로세서가 데이터를 가장 빠르게 읽어 들일 수 있는 물리적 저장장치는 무엇인가?
    1) 캐시                               - 정답
    2) 메인 메모리
    3) 플래시 메모리
    4) 가상 메모리
```

```
  2.  다음 중 디스크를 병렬적으로 구동함으로써 얻을 수 있는 효과가 아닌 것은?
    1) 스트라이핑 기법으로 디스크를 병렬 구동하면 데이터 입출력을 더욱 빠르게 할 수 있다. 
    2) 디스크 병렬 구동 시 프로세서의 연산 속도가 배로 향상되므로 프로세서는 같은 시간에 더 많은 작업을 처리할 수 있다.          - 정답
    3) 미러링 기법으로 디스크를 병렬 구동하면 어느 한 디스크에 장애가 발생하더라도 그를 복구할 수 있다. 
    4) 때로는 에러 감지 코드를 저장함으로써 데이터 저장 신뢰성을 높일 수 있다.                          -
```

```
  3.  블럭 내에서 가변 길이 레코드를 관리하기 위하여 사용되는 구조는?
    1) 다이나믹 프로그래밍
    2) 계층적 구조 
    3) 슬롯 페이지 구조                      - 정
    4) 다중 테이블 군집 파일 구조
```

```
  4.  다음 중 버퍼와 관련된 설명으로 적합하지 않은 것은?
    1) 버퍼 관리자는 데이터베이스 관리자(DBA)와 마찬가지로 시스템에 대한 전문 지식을 갖춰야 한다.          - 정답
    2) 버퍼는 메인 메모리에 존재하는 공간이다.                                                           
    3) 버퍼에 더 이상 블럭을 적재할 수 없다면 버퍼 내에 존재하는 블럭 하나가 선택되어 디스크로 저장된다.     -
    4) 버퍼 교체 전략은 시스템 성능에 영향을 끼치는 요소가 되기도 한다. 
```

```
  5. 다음 중 데이터베이스, 파일, 블럭, 레코드를 크기가 큰 것에서 작은 순서로 나열된 것은? (단, 레코드는 블럭보다 작은 것으로 가정한다.)
    1) 데이터베이스  파일  블럭  레코드          - 정
    2) 파일  블럭  레코드  데이터베이스
    3) 데이터베이스  블럭  레코드  파일
    4) 파일  데이터베이스  블럭  레코드 
```

