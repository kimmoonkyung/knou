```
  1. SQL에서 탐색 대상 테이블을 기술하는 절은?
    1) FROM 절             - 정
    2) SEARCH 절
    3) WHERE 절  
    4) SELECT 절
```

> 2. 다음 SQL문에 대한 설명으로 올바르지 않은 것은? 
```sql
            SELECT 비디오번호
              FROM (SELECT 비디오번호, COUNT(비디오번호) AS 비디오개수           
                      FROM 비디오           
                     GROUP BY 비디오번호           
                    HAVING 비디오개수 > 10)
```

```
    1) 위 질의는 중첩 질의를 이용한 질의이다. 
    2) 각 비디오 번호에 해당하는 비디오가 여러 개 있을 것이다.
    3) DISTINCT 비디오 번호를 입력하면 결과가 달라질 것이다.           - 정
    4) 비디오 개수가 11개 이상인 비디오 번호가 출력될 것이다.   
```

```
  3. SQL에서 릴레이션 내의 중복을 제거하고자 할 때의 명령 구성을 바르게 설명한 것은?
    1) SELECT 절 다음에 DISTINCT를 기술한다.             - 정
    2) SELECT 절 다음에 RESTRICT를 기술한다.
    3) WHERE 절 다음에 DISTINCT를 기술한다.
    4) WHERE 절 다음에 RESTRICT를 기술한다.
```

```
  4. SQL에서 CREATE VIEW 명령을 바르게 설명한 것은?
    1) 전체 스키마를 생성한다.  
    2) 사용자 뷰를 생성한다.                         - 정
    3) 새로운 데이터를 생성하고 조작한다.  
    4) 새로운 인덱스를 생성한다. 
```
    
    
    
