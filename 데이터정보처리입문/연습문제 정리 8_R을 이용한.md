```
  1. 다음과 같은 텍스트 파일을 읽어 들이려고 한다. ㉠에 들어갈 R 명령은?         - 2번
            > ex.data = ( ㉠ )("c:/data/example.txt", header = T)  
```
> ![image](https://user-images.githubusercontent.com/17442343/171563812-9a7865d6-1490-48bf-8bec-9bb5e9cd7c59.png)
> 1) scan       2) read.table       3) data.frame     4) read.xlsx

```
  2. 데이터 객체의 처음 6개의 케이스를 출력하고자 한다. ㄱ에 들어갈 R 명령은 ?
            > ( ㄱ ) (ex.data)
    1) head           - 정
    2) list
    3) print
    4) output
```

```
  3. package xlsx를 인스톨 하였다. 이를 가동시키기 위한 명령은 ? 
    1) system(xlsx)
    2) use(xlsx)
    3) library(xlsx)            - 정답
    4) load(xlsx)          
```

```
  4. 데이터 객체 ex.data의 변수들을 직접 사용하고자 한다. 유용한 명령은 ?
    1) load(ex.data)              -
    2) detach(ex.data)
    3) unload(data)
    4) attach(ex.data)            - 정답
```

```
  5. 그룹 변수인 성별(sex)의 값에 따라 변수 salary의 평균(mean)을 구하는 명령은 ?
    1) sapply(salary, sex, mean)          -
    2) sapply(mean, sex, salary) 
    3) tapply(salary, sex, mean)          - 정답
    4) tapply(mean, sex, salary)
```

```
  6. 평균이 0, 분산이 1인 정규분포를 따르는 난수 100개를 생성하는 명령은 ?
    1) pnorm(100)
    2) dnorm(100)
    3) rnorm(100)           - 정
    4) qnorm(100)
```

```
  7. 상자 그림을 그리는 명령은 ?
    1) stem(ex.data)
    2) boxplot(ex.data)             - 정답
    3) box(data)
    4) box.plot(ex.data)
```

```
  8. 다음은 통계 소프트웨어 R에 대한 설명이다. 옳은 설명은 모두 몇 개 인가 ?
              1. 인터넷에서 자유롭게 다운 받아 사용할 수 있는 장점이 있다.            - 정
              2. R은 대화형 언어(interpreted langauge)이다.
              3. 대표적인 스프레드시트 프로그램으로 차트 작성에 강점이 있다.          - 정
              4. R은 객체지향 시스템이다.                                          - 정
    1) 1
    2) 2          
    3) 3          - 정
    4) 모두
```

```
  9. 다음 R 명령 수행결과는?
              > x = c(-1, 0, 1)
              > y = c(1, 2, 3)
              > z = c(4, 5, 6)
              > dframe = data.frame(x, y, z)
              > dframe[2, 3]
    1) 1     
    2) 3 
    3) 5            - 정    
    4) 6
```

```246p
  10. 다음과 같은 R 명령어를 입력한 결과로 알맞은 것은 ?
            > x = (-3:3)          --------->  -3, -2, -1, 0, 1, 2, 3
            > y = x[x>1]          --------->  2, 3
            > sum(y^2)            ---------> (2*2) + (3*3)
    1) 4
    2) 9
    3) 13     - 정
    4) 14
```

```
  11. 다음은 데이터를 불러와 분석하는 R 프로그램의 일부이다. 다음 설명 중에서 옳지 않은 것은?
            > score <- scan("c:/data/t3.txt") 
             Read 20 items
            > stem(score)
    1) “c:/data/t3.txt”에 저장되어 있는 데이터를 읽어서 변수명을 score로 하였다.
    2) 데이터는 20개의 관측치로 구성되어 있다.
    3) stem(score) 명령에 의하여 상자그림을 얻을 수 있다.                           - 정
    4) stem(score) 명령은 "연속형 자료의 분포 모양을 파악하고자 할 때 사용"된다.
```

```
  12. 다음 R 명령 수행결과는?
            > x = c("red", "green", "blue")
            > y = c(1, 2, 3)
            > z = c(4, 5, 6)
            > dframe = data.frame(x, y, z)
            > dframe[1, 2]
    1) 1                                              - 정
    2) red    
    3) 4 
    4) green 
```

```248p
  13. 3. 다음과 같은 R 명령어를 입력한 결과로 알맞은 것은?            - 정답 2번
            > y = matrix(c(1,2,3,4), ncol = 2, byrow = T)
            > t(y)
    1)         [,1]  [,2]             2)      [,1]  [,2]  
       [1,]      1     2                [1,]     1    3  
       [2,]      3     4                [2,]     2    4
    3)         [,1]  [,2]             4)      [,1]  [,2]  
       [1,]      1     4                [1,]     1    2  
       [2,]      2     3                [2,]     4    3
```

```
  14. 다음 R 명령 수행 결과는 ?
            > x = c(-1, 0, 1)
            > y = c(1, 2, 3)
            > z = c(4, 5, 6)
            > df = data.frame(x, y, z)
            > df[2, 3] + df[3, 3]
                ------------> 5 + 6
    1) 5 
    2) 8 
    3) 9 
    4) 11           - 정
```

```
  15. 다음과 같은 R 명령어를 입력한 결과로 알맞은 것은 ?
              > x = 2*(-4:4)                              ----------> -8, -6, -4, -2, 0, 2, 4, 6, 8
              > y = x[x>3]                                ----------> 4, 6, 8
              > median = median(y)                        ----------> 6
              > sum((y-median)^2)                         ----------> ((4-6)^2) + ((6-6)^2) + ((8-6)^2) ----> 4 + 0 + 4
    1) 0 
    2) 5 
    3) 8                    - 정
    4) 10
```

```
  16. 다음은 R 프로그램의 일부이다. 이 프로그램을 통해서 얻는 분석 결과는 무엇인가? 
            > math <- c(66, 64, 48, 78, 60, 90, 50, 66, 70)
            > physics <- c(70, 69, 46, 84, 64, 92, 52, 68, 72)
            > plot(math, physics)
    1) 두 변수 math와 physics 사이의 상관계수를 구한다.
    2) 두 변수 math와 physics 사이의 산점도를 그린다.             - 정답
    3) 두 변수 math와 physics에 대한 상자그림을 그린다.
    4) 두 변수 math와 physics 사이의 단순회귀식을 적합한다.
```

```
  17. 다음과 같은 R 명령어를 입력한 결과로 알맞은 것은?
            > x = 2 * (-5:5)                        ---------> -10, -8, -6, -4, -2, 0, 2, 4, 6, 8, 10
            > y = x[x>2]                            ---------> 4, 6, 8, 10
            > mean = mean(y)                        ---------> 7
            > sum((y-mean)^2)                       ---------> ((4-7)^2) + ((6-7)^2) + ((8-7)^2) + ((10-7)^2) ----> 9 + 1 + 1 + 9
    1) 10 
    2) 15  
    3) 18
    4) 20             - 정
```
